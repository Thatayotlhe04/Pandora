# Pandora Landing Page

Static landing page for Pandora. It is deployed from `site/` by the GitHub Pages
workflow.

Preview locally:

```bash
python3 -m http.server 4173 --directory site
```

Open `http://localhost:4173`.
